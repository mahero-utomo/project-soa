<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ApiMedic - Register</title>
    <link href="/img/Medical Check Up Icons.png" rel="icon">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/css/LoginRegister.css">
</head>
<body>
    <h1>ApiMedic</h1>
    <div class="form-input register">
        <form action="/developer/register" method="post">
            <i class="fa fa-heartbeat"></i>
            <h2>Register</h2>
            <div class="group">
                <input type="text" name="email" placeholder="Email">
                <i class="fa fa-envelope"></i>
                <% if (typeof errorResult != 'undefined' && errorResult.email) { %>
                    <label for=""><%= errorResult.email %></label>
                <% } %>
            </div>
            <div class="group">
                <input type="text" name="username" placeholder="Username">
                <i class="fa fa-user"></i>
                <% if (typeof errorResult != 'undefined' && errorResult.username) { %>
                    <label for=""><%= errorResult.username %></label>
                <% } %>
            </div>
            <div class="group">
                <input type="text" name="name" placeholder="Name">
                <i class="fa fa-address-card"></i>
                <% if (typeof errorResult != 'undefined' && errorResult.name) { %>
                    <label for=""><%= errorResult.name %></label>
                <% } %>
            </div>
            <div class="group">
                <input type="password" name="password" placeholder="Password">
                <i class="fa fa-lock"></i>
                <% if (typeof errorResult != 'undefined' && errorResult.password) { %>
                    <label for=""><%= errorResult.password %></label>
                <% } %>
            </div>
            <button><i class="fa fa-user-plus"></i> Register</button>
            <p class="fs">Already have an account ? <a href="/login">Sign in</a></p>
        </form>
    </div>
</body>
</html>